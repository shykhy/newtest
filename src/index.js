import dva from 'dva';
import './index.css';

sas
mac update:
this as
how are you

this is key sy

// 1. Initialize
const app = dva();   
hhh
// 2. Plugins
// app.use({}); 

// 3. Model
// app.model(require('./models/example').default); 
bbbb
// 4. Router
app.router(require('./router').default);
 
// 5. Start
app.start('#root');

last one
